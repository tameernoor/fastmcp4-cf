node_modules/
.wrangler/

# Local config — never commit. `.dev.vars.example` is the template.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example

# Cloudflare Access service-token credentials, for non-interactive probing.
.svc-token
.deploy.vars

__pycache__/
*.py[cod]
.venv/
venv/
stash/
.DS_Store

# Working notes from a security review. Kept out of the repo: it is a map of
# weaknesses against a specific commit, and stale once they are fixed.
SECURITY_FINDINGS_AND_MITIGATIONS.md
